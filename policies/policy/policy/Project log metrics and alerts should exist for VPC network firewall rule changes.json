{
  "properties": {
    "displayName": "Project log metrics and alerts should exist for VPC network firewall rule changes",
    "policyType": "BuiltIn",
    "mode": "All",
    "description": "Deny creation or update of VPC network firewall rules if log metrics and alerts for firewall rule changes do not exist.",
    "metadata": {
      "version": "1.0.0",
      "category": "Networking"
    },
    "parameters": {},
    "policyRule": {
      "if": {
        "allOf": [
          {
            "field": "type",
            "equals": "Microsoft.Network/networkSecurityGroups"
          },
          {
            "anyOf": [
              {
                "field": "Microsoft.Network/networkSecurityGroups/diagnosticSettings[*].logs[*].enabled",
                "notEquals": true
              }
            ]
          }
        ]
      },
      "then": {
        "effect": "deny"
      }
    }
  }
}